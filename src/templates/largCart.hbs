{{#each this}}
  <article class="app_cart">
    <div class="head">
      <img src={{app_img}} class="company_icon" alt=""/>
      <ul>
        <li class="name">{{title}}</li>
        <li class="status">{{discount}}</li>
        <li class="cart_desc">{{price}}</li>
      </ul>
    </div>
        <a class="btn" href=""></a>
    </div>
  </article>

{{/each}}